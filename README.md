TypeScript React Webpack <img> Embed Images Demo
================================================

如何把一个图片以`base64`形式嵌入到一个`<img>`中。

```
npm install
npm run demo
```

It will open page on browser automatically.
